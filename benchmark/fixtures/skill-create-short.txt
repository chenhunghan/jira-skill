acli jira workitem create \
  --summary 'Settings profile Save button returns HTTP 500' \
  --project BENCH \
  --type Bug \
  --description-file <(mdadf --compact <<'MD'
**Short Description**

The Save button on the settings profile page returns HTTP 500 after the 1.42 release.

**Steps to Reproduce**

* Open `/settings/profile`
* Click the **Save** button
MD
)
