{
  "skill_name": "jira",
  "evals": [
    {
      "id": 1,
      "prompt": "Show me MYPROJECT-1455 with just the summary and comments.",
      "expected_output": "Uses a direct view command for the specific key with a focused fields list and stays read-only.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem view MYPROJECT-1455` rather than a JQL search.",
        "Uses a focused fields selection such as `summary,comment` instead of default broad output.",
        "Does not propose or run a mutating Jira command."
      ]
    },
    {
      "id": 2,
      "prompt": "Create a bug titled \"Fix broken validation response\" assigned to me. The description should have a heading and a bullet list explaining why this matters.",
      "expected_output": "Creates a bug in the configured default project, self-assigns it, and uses `mdadf --compact` with `--description-file` for the rich description.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem create` with `--type \"Bug\"` and `--project \"MYPROJECT\"` (or the resolved default).",
        "Uses `--assignee \"@me\"` or an equivalent self-assignment flag.",
        "Uses `mdadf --compact` with `--description-file` rather than raw Markdown or inline JSON."
      ]
    },
    {
      "id": 3,
      "prompt": "Add a comment to MYPROJECT-801 saying the issue is reproduced in dev and logs are attached, formatted as a bulleted update.",
      "expected_output": "Creates a comment on the specific key and uses `mdadf --compact` with `--body-file`.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem comment create --key \"MYPROJECT-801\"`.",
        "Uses `mdadf --compact` with `--body-file`.",
        "Does not use JQL for a single known work item key."
      ]
    },
    {
      "id": 4,
      "prompt": "Update comment 10001 on MYPROJECT-801 to say the fix is ready for QA, using a heading and a short paragraph.",
      "expected_output": "Updates the specific comment ID on the specific work item and uses `mdadf --compact` with `--body-adf`.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem comment update --key \"MYPROJECT-801\" --id \"10001\"`.",
        "Uses `mdadf --compact` with `--body-adf`.",
        "Does not replace the ADF flow with plain text when the prompt asks for rich formatting."
      ]
    },
    {
      "id": 5,
      "prompt": "Move MYPROJECT-733 to In Review and assign it to me.",
      "expected_output": "Uses key-based transition and assignment commands for the same work item without falling back to JQL.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem transition --key \"MYPROJECT-733\" --status \"In Review\"` or an equivalent key-based transition.",
        "Uses `acli jira workitem assign --key \"MYPROJECT-733\" --assignee \"@me\"` or an equivalent self-assignment command.",
        "Does not use JQL for a single known work item key."
      ]
    },
    {
      "id": 6,
      "prompt": "Find all bugs assigned to me in the current sprint that are still In Progress.",
      "expected_output": "Uses JQL search with project from config, assignee = currentUser(), sprint in openSprints(), type = Bug, status = In Progress.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem search --jql` with appropriate JQL filters.",
        "Includes `project = MYPROJECT` (or the resolved default) in the JQL query.",
        "Does not attempt a key-based command."
      ]
    },
    {
      "id": 7,
      "prompt": "File a bug: our login page returns a 500 when you submit an empty email field. I'm on Chrome 120, macOS Sonoma. Assign it to me.",
      "expected_output": "Creates a bug with bold section labels, not headings. Derives reproduction steps from the user's description.",
      "files": [],
      "expectations": [
        "Uses `**Short Description**` bold labels, not `## Short Description` headings.",
        "Includes steps to reproduce derived from the user's description.",
        "Uses `--assignee \"@me\"`.",
        "Uses `mdadf --compact` with `--description-file`."
      ]
    },
    {
      "id": 8,
      "prompt": "I'm on Windows PowerShell. Create a task in MYPROJECT called 'Update API docs' with a bulleted description listing three endpoints to document.",
      "expected_output": "Recognizes Windows environment and avoids bash process substitution. Uses a temp file pattern.",
      "files": [],
      "expectations": [
        "Does not use `<(...)` process substitution.",
        "Uses `mdadf --compact` to convert markdown.",
        "Writes ADF to a temp file and passes it via `--description-file`."
      ]
    },
    {
      "id": 9,
      "prompt": "Move MYPROJECT-500 to Done.",
      "expected_output": "Fetches the current status of MYPROJECT-500 before attempting the transition, per safety rule 1.",
      "files": [],
      "expectations": [
        "Runs `acli jira workitem view MYPROJECT-500` (or equivalent status check) BEFORE the transition command.",
        "Uses `acli jira workitem transition --key \"MYPROJECT-500\" --status \"Done\"` or equivalent.",
        "Does not blindly transition without verifying current state."
      ]
    },
    {
      "id": 10,
      "prompt": "Update the description of MYPROJECT-200 to say the feature is no longer needed.",
      "expected_output": "Shows the current description before replacing it, per safety rule 3.",
      "files": [],
      "expectations": [
        "Shows an `acli jira workitem view MYPROJECT-200` command (or equivalent) BEFORE the edit command.",
        "Uses `acli jira workitem edit --key \"MYPROJECT-200\"` with `--description-file` and `mdadf --compact`.",
        "The view command appears before the edit command in the response, not after."
      ]
    },
    {
      "id": 11,
      "prompt": "Close all 12 bugs in the current sprint that are marked Resolved.",
      "expected_output": "Warns about bulk modification and asks for explicit user approval before proceeding, per safety rule 2.",
      "files": [],
      "expectations": [
        "Uses JQL to find the matching bugs first.",
        "Warns the user about the number of items to be modified and requests approval before executing transitions.",
        "Does not silently transition all 12 items without confirmation."
      ]
    },
    {
      "id": 12,
      "prompt": "Create a critical P1 bug with Business Urgency set to Blocker for a login crash.",
      "expected_output": "Creates the bug but does NOT set Priority, Technical Impact, or Business Urgency fields, per safety rule 5.",
      "files": [],
      "expectations": [
        "Creates a bug with `acli jira workitem create` using the structured bold-label template.",
        "Does NOT include `--priority`, Technical Impact, or Business Urgency flags.",
        "Explains that priority/urgency fields are left for triage."
      ]
    },
    {
      "id": 13,
      "prompt": "Create a task called 'Refactor auth module'. I haven't configured my project yet.",
      "expected_output": "Asks the user for their project key and writes the answer to .jira-skill.json or ~/.config/jira-skill/config.json.",
      "files": [],
      "expectations": [
        "Asks the user which project key to use before creating the task.",
        "Writes the project key to `.jira-skill.json` (workspace) or `~/.config/jira-skill/config.json` (global), not the in-repo config.json."
      ]
    },
    {
      "id": 14,
      "prompt": "Find all MYPROJECT bugs that changed status to Done this week, assigned to anyone on the dev-team group.",
      "expected_output": "Builds a JQL query using CHANGED, startOfWeek(), and membersOf() — patterns from the reference file, not the common patterns in SKILL.md.",
      "files": [],
      "expectations": [
        "Uses `acli jira workitem search --jql` with a JQL query.",
        "Uses `status CHANGED TO \"Done\"` or equivalent historical operator.",
        "Uses `startOfWeek()` or `AFTER -7d` for the time range.",
        "Uses `membersOf(\"dev-team\")` or equivalent group function."
      ]
    }
  ]
}
